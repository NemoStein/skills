---
name: worktrees
description: Shared runtime for the isolate, integrate, and discard managed-worktree skills. Install it alongside those skills from the same source.
disable-model-invocation: true
---

# Worktree Package

Install this support skill with `isolate`, `integrate`, and `discard`. It provides their shared lifecycle runtime and reference contract.
